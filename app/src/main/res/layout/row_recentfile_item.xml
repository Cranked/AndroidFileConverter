<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.cranked.androidfileconverter.adapter.recentfile.RecentFileAdapterViewModel" />
    </data>

    <LinearLayout
        android:id="@+id/recentFileLinearLayout"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:contextClickable="true"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/recentFileImage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            app:srcCompat="@drawable/custom_radius_folder" />

        <TextView
            android:id="@+id/recentFileName"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxWidth="150dp"
            android:maxLines="1"
            android:singleLine="true"
            android:text="TextViewawdawdawdawdawdawd" />


    </LinearLayout>

</layout>